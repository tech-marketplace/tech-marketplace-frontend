const homeIconConfig = {
    showonRoutes: ['/product-details', '/cart', '/request-form', '/purchased-items','/profile'], // Routes where the login button should be shown
  };
  
  export default homeIconConfig;