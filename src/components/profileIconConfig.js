const profileIconConfig = {
    showonRoutes: ['/product-details', '/cart', '/request-form', '/purchased-items'], // Routes where the login button should be shown
  };
  
  export default profileIconConfig;